resources:
  jobs:
    load_contract_job:
      name: load_contract_job
      tasks:
        - task_key: load_contract
          notebook_task:
            notebook_path: ../notebooks/01_bronze/load_contract.pynb
          new_cluster:
            spark_version: 13.3.x-scala2.12
            node_type_id: Standard_DS3_v2
            num_workers: 1
