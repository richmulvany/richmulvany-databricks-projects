resources:
  jobs:
    load_contract_job:
      name: load_contract_job
      tasks:
        - task_key: load_contract
          notebook_task:
            notebook_path: ../notebooks/01_bronze/load_contract.py
