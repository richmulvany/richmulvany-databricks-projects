name: Deploy and Run WarcraftLogs Job

on:
  push:
    paths:
      - 'warcraftlogs-raid-analysis/**'
      - '.github/workflows/deploy-warcraftlogs.yml'

jobs:
  deploy-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: 🛠️ Install Databricks CLI
        uses: databricks/setup-cli@main

      - name: 💉 Inject job YAML into databricks.yml
        working-directory: warcraftlogs-raid-analysis
        run: |
          export JOB_DEFINITION="$(< jobs/bronze-ingestion-extraction-job.yaml)"
          echo "$JOB_DEFINITION"
          envsubst < databricks.yml.template > databricks.yml
        env:
          DATABRICKS_HOST_ADDRESS: ${{ vars.DATABRICKS_HOST_ADDRESS }}

      - name: 🚀 Deploy and Run Asset Bundle
        working-directory: warcraftlogs-raid-analysis
        run: |
          databricks bundle deploy
          databricks bundle run warcraft-job --refresh-all
        env:
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_BUNDLE_ENV: default
          DATABRICKS_CLUSTER_ID: ${{ secrets.DATABRICKS_CLUSTER_ID }}
